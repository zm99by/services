#Sevices K8S

```
apiVersion: v1
kind: Service
metadata:
  name: my-nginx
  labels:
    run: my-nginx
spec:
  ports:
  - port: 8081
    protocol: TCP
  selector:
    run: my-nginx
```

```
apiVersion: apps/v1
kind: service
metadata:
  name: my-nginx
  labels:
    run: my-nginx
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: my-nginx
```    
    
```
apiVersion: apps/v1betta1
kind: Service
metadata:
  name: my-nginx
  labels:
    run: my-nginx
spec:
  ports:
  - port: 8081
    protocol: TCP
  selector:
    run: my-nginx
```
